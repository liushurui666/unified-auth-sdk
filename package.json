{
  "name": "unified-auth-workspace",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.4.0",
  "scripts": {
    "build": "pnpm --filter @rc-tool/unified-auth-sdk build && pnpm --filter @rc-tool/unified-auth-hosted-service build",
    "clean": "pnpm -r clean",
    "prepack": "pnpm build",
    "publish:packages": "pnpm build && pnpm -r --filter './packages/*' publish --access public --no-git-checks",
    "test": "pnpm -r test",
    "typecheck": "pnpm build"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@types/pg": "^8.20.0",
    "@types/react": "^19.2.14",
    "better-auth": "^1.6.14",
    "drizzle-orm": "^0.45.2",
    "react": "^19.2.3",
    "typescript": "^5.9.3",
    "vitest": "^4.1.7"
  }
}
